@model PagingViewModel
@{
    ViewData["Title"] = "TailwindAndDaisy View (Default)";
    var previewModel = new PreviewPartialViewModel();
    previewModel.Description = "This example demonstrates the default TailwindAndDaisy view using DaisyUI components. This is the recommended default view that provides the best experience with HTMX and full dark mode support.";
    previewModel.SourceLink="/Views/Home/TailwindView.cshtml";
}

<partial name="_PreviewPartial" model="previewModel"/>

<paging model="Model"
        class="my-4">
</paging>

<partial name="_ResultsList" model="Model"/>

@Html.PageSizeOnchangeSnippet()
