<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, height=device-height, initial-scale=1.0"/>
    <title>@ViewData["Title"] - Pure Tailwind Demo</title>

    <!-- Pure Tailwind CSS (via CDN for demo isolation) -->
    <script src="https://cdn.tailwindcss.com"></script>

    <link rel="icon" type="image/png" href="/img/smalllogo.svg" />

    <script>
        // Configure Tailwind to match the project's setup but without DaisyUI
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {}
            }
        }
    </script>
</head>
<body class="flex flex-col min-h-screen bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-gray-100">
<header class="w-full bg-white dark:bg-gray-800 shadow-md py-3 px-6 flex justify-between items-center">
    <!-- Logo -->
    <a href="https://www.mostlylucid.net" class="flex items-center gap-2 text-lg font-bold">
        <img src="/img/logo.svg" alt="Logo" height="56" class="invert dark:invert-0">
    </a>

    <!-- Navigation Links -->
    <nav class="flex gap-4">
        <a href="/" class="inline-flex items-center px-4 py-2 text-sm font-medium text-blue-600 bg-white border border-blue-600 rounded-md hover:bg-blue-50 dark:bg-gray-800 dark:text-blue-400 dark:border-blue-400 dark:hover:bg-gray-700 transition-colors">
            ← Back to Demos
        </a>
        <a href="https://github.com/scottgal/mostlylucid.pagingtaghelper" target="_blank" class="inline-flex items-center px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50 dark:bg-gray-800 dark:text-gray-200 dark:border-gray-600 dark:hover:bg-gray-700 transition-colors">
            GitHub
        </a>
        <button onclick="toggleTheme()" class="inline-flex items-center px-4 py-2 text-sm font-medium text-white bg-blue-600 border border-blue-600 rounded-md hover:bg-blue-700 dark:bg-blue-500 dark:hover:bg-blue-600">
            <span class="light-mode">☀️ Light</span>
            <span class="dark-mode hidden">🌙 Dark</span>
        </button>
    </nav>
</header>

<div class="flex justify-center mt-4">
    <main role="main" class="flex-1 w-full max-w-7xl mx-auto px-4">
        @RenderBody()
    </main>
</div>

<script src="https://unpkg.com/htmx.org@2.0.4"
        integrity="sha384-HGfztofotfshcF7+8n44JQL2oJmowVChPTg48S+jvZoztPfvwD79OC/LTtG6dMp+"
        crossorigin="anonymous"></script>

@await RenderSectionAsync("Scripts", required: false)

<script>
    function setTheme(theme) {
        const html = document.documentElement;
        const isDark = theme === 'dark';

        html.classList.toggle('dark', isDark);
        localStorage.setItem('theme', theme);

        document.querySelector('.light-mode')?.classList.toggle('hidden', isDark);
        document.querySelector('.dark-mode')?.classList.toggle('hidden', !isDark);
    }

    function toggleTheme() {
        const isDark = document.documentElement.classList.contains('dark');
        setTheme(isDark ? 'light' : 'dark');
    }

    function initTheme() {
        const savedTheme = localStorage.getItem('theme');
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;

        if (savedTheme === 'dark' || (!savedTheme && prefersDark)) {
            setTheme('dark');
        } else {
            setTheme('light');
        }
    }

    document.addEventListener('DOMContentLoaded', initTheme);
    window.toggleTheme = toggleTheme;
</script>

<script defer src="https://umami.mostlylucid.net/getinfo" data-website-id="7c569b68-6c21-45ce-8413-bfdfff76ff9d"></script>
</body>
</html>
